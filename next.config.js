/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configurations can go here.
  // We've fixed all the errors, so you don't need to ignore them anymore.
};

module.exports = nextConfig;